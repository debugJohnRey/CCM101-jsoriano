# Checkpoint 5 - The Container Lifecycle
| Command | Description |
| :--- | :--- |
| `docker ps` | This command lists active containers. |
| `docker stop <container_id>` | This command stops the running container. |
| `docker ps -a` | This command shows all containers. |
| `docker rm <container_id>` | This command deletes the stopped container. |